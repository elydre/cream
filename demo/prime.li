:to_test div is_prime

to_test = 1

for to_test (0, 100) {
    is_prime = 1
    for div (2, to_test) {
        if to_test div % 0 == {
            is_prime = 0
            break
        }
    }
    if is_prime 1 == {
        dump(to_test)
    }
}
