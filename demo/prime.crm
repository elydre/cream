:to_test
:div
:is_prime

to_test = 1

while to_test 100 < {
    div = 2
    is_prime = 1
    while div to_test < {
        if to_test div % 0 == {
            is_prime = 0
            break
        }
        div = div 1 +
    }
    if is_prime 1 == {
        dump(to_test)
    }
    to_test = to_test 1 +
}
